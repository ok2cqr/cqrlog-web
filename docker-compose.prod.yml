services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: con-app:latest
    restart: unless-stopped
    env_file:
      - .env.prod
    environment:
      APP_ENV: prod
      APP_DEBUG: 0
    ports:
      - "${APP_PORT:-8080}:80"
